export const metadata = {
  title: "License Management | Admin Dashboard",
  description: "Configure and manage your license identity and assets.",
};

export default function LicenseLayout({ children }) {
  return <>{children}</>;
}
