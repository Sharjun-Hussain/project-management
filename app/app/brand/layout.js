export const metadata = {
  title: "Brand Management | Admin Dashboard",
  description: "Configure and manage your brand identity and assets.",
};

export default function BrandLayout({ children }) {
  return <>{children}</>;
}
