export const metadata = {
  title: "Orders Management | Admin Dashboard",
  description: "Track and manage process customer orders.",
};

export default function OrdersLayout({ children }) {
  return <>{children}</>;
}
