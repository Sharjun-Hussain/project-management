export const metadata = {
  title: "Support Ticket Support Tickets | Admin Dashboard",
  description: "Manage customer messages and support_ticket support_tickets.",
};

export default function SupportTicketsLayout({ children }) {
  return <>{children}</>;
}
