export const metadata = {
  title: "Coupons & Discounts | Admin Dashboard",
  description: "Manage promotional coupons and discount rules.",
};

export default function CouponsLayout({ children }) {
  return <>{children}</>;
}
