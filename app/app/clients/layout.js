export const metadata = {
  title: "Clients Management | Admin Dashboard",
  description: "View and manage client profiles and details.",
};

export default function ClientsLayout({ children }) {
  return <>{children}</>;
}
