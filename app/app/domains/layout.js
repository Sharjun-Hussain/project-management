export const metadata = {
  title: "Domains Management | Admin Dashboard",
  description: "Manage product domains for better organization.",
};

export default function SubCategoriesLayout({ children }) {
  return <>{children}</>;
}
