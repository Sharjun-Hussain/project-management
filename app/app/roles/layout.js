export const metadata = {
  title: "Roles Management | Admin Dashboard",
  description: "Configure and assign user roles and group permissions.",
};

export default function RolesLayout({ children }) {
  return <>{children}</>;
}
