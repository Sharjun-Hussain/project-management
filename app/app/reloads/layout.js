export const metadata = {
  title: "Reloads Manager | Project Dashboard",
  description: "Manage and track reloads and their expiry dates",
};

export default function ReloadsLayout({ children }) {
  return children;
}
