export const metadata = {
  title: "Contact Inquiries | Admin Dashboard",
  description: "Manage customer messages and contact inquiries.",
};

export default function ContactsLayout({ children }) {
  return <>{children}</>;
}
