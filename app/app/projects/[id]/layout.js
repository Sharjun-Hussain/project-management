export const metadata = {
  title: "Edit Project | Admin Dashboard",
  description: "Modify and update project details and specifications.",
};

export default function EditProjectLayout({ children }) {
  return <>{children}</>;
}
