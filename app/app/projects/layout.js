export const metadata = {
  title: "Projects Management | Admin Dashboard",
  description: "View and manage your project catalog and inventory.",
};

export default function ProjectsLayout({ children }) {
  return <>{children}</>;
}
