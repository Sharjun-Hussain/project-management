export const metadata = {
  title: "Add New Project | Admin Dashboard",
  description: "Create and add a new project to your inventory.",
};

export default function NewProjectLayout({ children }) {
  return <>{children}</>;
}
