export const metadata = {
  title: "Delivery Process Settings | CMS Management",
  description: "Configure the delivery process walkthrough sections.",
};

export default function DeliveryLayout({ children }) {
  return <>{children}</>;
}
