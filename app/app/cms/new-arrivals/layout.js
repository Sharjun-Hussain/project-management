export const metadata = {
  title: "New Arrivals Settings | CMS Management",
  description: "Configure new arrivals sections and displays.",
};

export default function NewArrivalsLayout({ children }) {
  return <>{children}</>;
}
