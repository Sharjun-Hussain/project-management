export const metadata = {
  title: "Hero Banner Settings | CMS Management",
  description: "Manage homepage hero banners and sliders.",
};

export default function HeroLayout({ children }) {
  return <>{children}</>;
}
