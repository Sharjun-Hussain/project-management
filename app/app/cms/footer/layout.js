export const metadata = {
  title: "Footer Settings | CMS Management",
  description: "Configure and manage website footer content.",
};

export default function FooterLayout({ children }) {
  return <>{children}</>;
}
