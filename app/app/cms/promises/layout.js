export const metadata = {
  title: "Promises & Benefits | CMS Management",
  description: "Manage website promises and benefit sections.",
};

export default function PromisesLayout({ children }) {
  return <>{children}</>;
}
