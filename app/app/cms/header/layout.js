export const metadata = {
  title: "Header Settings | CMS Management",
  description: "Configure website navigation header and top bars.",
};

export default function HeaderLayout({ children }) {
  return <>{children}</>;
}
