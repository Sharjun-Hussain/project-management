export const metadata = {
  title: "Featured Sections Settings | CMS Management",
  description: "Manage homepage featured sections and content blocks.",
};

export default function FeaturedSectionsLayout({ children }) {
  return <>{children}</>;
}
