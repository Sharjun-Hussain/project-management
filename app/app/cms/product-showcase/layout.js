export const metadata = {
  title: "Product Showcase Settings | CMS Management",
  description: "Configure featured product showcases and displays.",
};

export default function ProductShowcaseLayout({ children }) {
  return <>{children}</>;
}
