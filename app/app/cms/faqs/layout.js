export const metadata = {
  title: "FAQ Settings | CMS Management",
  description: "Manage frequently asked questions and answers.",
};

export default function FAQsLayout({ children }) {
  return <>{children}</>;
}
