export const metadata = {
  title: "CMS Management | Admin Dashboard",
  description: "Manage website content, banners, and sections.",
};

export default function CMSLayout({ children }) {
  return <>{children}</>;
}
