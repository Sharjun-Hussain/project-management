export const metadata = {
  title: "Collection Settings | CMS Management",
  description: "Manage product collections and category displays.",
};

export default function CollectionsLayout({ children }) {
  return <>{children}</>;
}
