export const metadata = {
  title: "Contact Page Settings | CMS Management",
  description: "Manage contact page content and information.",
};

export default function ContactLayout({ children }) {
  return <>{children}</>;
}
