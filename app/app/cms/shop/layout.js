export const metadata = {
  title: "Shop Settings | CMS Management",
  description: "Configure shop page settings and layouts.",
};

export default function ShopLayout({ children }) {
  return <>{children}</>;
}
