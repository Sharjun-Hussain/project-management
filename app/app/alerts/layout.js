export const metadata = {
  title: "Product Alert Logs | Admin Dashboard",
  description: "Moderate and manage customer product alerts.",
};

export default function AlertLogsLayout({ children }) {
  return <>{children}</>;
}
