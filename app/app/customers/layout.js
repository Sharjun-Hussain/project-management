export const metadata = {
  title: "Customers Management | Admin Dashboard",
  description: "View and manage customer profiles and details.",
};

export default function CustomersLayout({ children }) {
  return <>{children}</>;
}
