export const metadata = {
  title: "Marketing Campaigns | Admin Dashboard",
  description: "Create and track marketing campaigns.",
};

export default function CampaignsLayout({ children }) {
  return <>{children}</>;
}
