export const metadata = {
  title: "Sub-Categories Management | Admin Dashboard",
  description: "Manage product sub-categories for better organization.",
};

export default function SubCategoriesLayout({ children }) {
  return <>{children}</>;
}
