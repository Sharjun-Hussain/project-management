export const metadata = {
  title: "Edit Product | Admin Dashboard",
  description: "Modify and update product details and specifications.",
};

export default function EditProductLayout({ children }) {
  return <>{children}</>;
}
