export const metadata = {
  title: "Add New Product | Admin Dashboard",
  description: "Create and add a new product to your inventory.",
};

export default function NewProductLayout({ children }) {
  return <>{children}</>;
}
