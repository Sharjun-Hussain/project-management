export const metadata = {
  title: "Products Management | Admin Dashboard",
  description: "View and manage your product catalog and inventory.",
};

export default function ProductsLayout({ children }) {
  return <>{children}</>;
}
