export const metadata = {
  title: "System Settings | Admin Dashboard",
  description: "Configure global system settings and configurations.",
};

export default function SettingsLayout({ children }) {
  return <>{children}</>;
}
