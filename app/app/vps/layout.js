export const metadata = {
  title: "VPS Hosting Management | Admin Dashboard",
  description: "Organize products into main vps.",
};

export default function VPSHostingLayout({ children }) {
  return <>{children}</>;
}
