export const metadata = {
  title: "Renewals Management | Admin Dashboard",
  description: "Track and manage process customer renewals.",
};

export default function RenewalsLayout({ children }) {
  return <>{children}</>;
}
